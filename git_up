#!/bin/bash

#bash_script git push
echo "Pushing files for bash scripting....\n"
git add *
git commit -a
git push

#career_portal git push
cd ../car_web/kayveez_car_web/
echo "Pushing files for career web job portal....\n"
git add *
git commit -a
git push

#learning webjs git push
cd /mnt/c/codes/js/
echo "Pushing files for java script web development"
git add *
git commit -a
git push
